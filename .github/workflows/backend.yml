name: Backend

on:
  push:
    branches: [main, development, feature/backend/stable]
    paths: ['backend/**', 'worker/**', '.github/workflows/backend.yml']
  pull_request:
    branches: [main, development, feature/backend/stable]
    paths: ['backend/**', 'worker/**', '.github/workflows/backend.yml']

jobs:
  build:
    name: Restore · Build · Test (C# .NET)
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-dotnet@v4
        with:
          dotnet-version: 10.0.x

      - run: dotnet restore LiftOff.slnx
      - run: dotnet build LiftOff.slnx --no-restore -c Release
      - run: dotnet test LiftOff.slnx --no-build -c Release

  worker-docker:
    name: Verify Worker Docker Build
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Build Worker Docker Image
        run: docker build -f worker/Dockerfile .

  python-tests:
    name: Run Python hiring-agent Tests
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: backend/src/AI/hiring-agent
    steps:
      - uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt

      - name: Run unit tests
        run: |
          python -m unittest discover -s tests -p "test_*.py"
